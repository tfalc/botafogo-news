# Atualização de tabelas

Fontes permitidas e aliases de nomes: [`standings_sources.json`](standings_sources.json).

Uso local (CRM ou CLI) grava em `content/standings/*.json` e recalcula objetivos.
Não baixa escudos — só números e nomes.
